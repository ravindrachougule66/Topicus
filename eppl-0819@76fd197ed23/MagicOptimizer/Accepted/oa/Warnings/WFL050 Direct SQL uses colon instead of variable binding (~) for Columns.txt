Program ISN,Program #,Task,Name,SQL
105,,".1","Monitor: count # records","select count(*) from ordersuitvoeringen u , orders o where (u.UIT_DATUMTRANS_AANG =  '00000000'    AND u.UIT_ORDERREGEL >=  1  :1AND o.ORD_ORDERNUMMER=u.UIT_ORDERNUMMER AND o.ORD_ORDERREGEL=u.UIT_ORDERREGEL)  AND ((o.ORD_ORDERTRAJEKT>= ~2   AND o.ORD_ORDERTRAJEKT<= ~3  ))"
1375732362,,".2","setResponse","select * FROM ( select * FROM trade_letter_header_vw WHERE :1 ORDER BY NO desc, VERSION desc ) A :2"
552,,".5.1","Alleen DEPOT","select * from (select A.ORDERTYPE, A.ORDERNUMMER, A.ORDERREGEL, A.ADVISEUR, A.RELATIE, A.DEPOT, A.FONDSSYMBOOL, A.OPTIETYPE, A.EXPIRATIE, A.EXERCISE, A.TRANSACTIESOORT, A.ORDERSOORT, A.ORDERKEUZE, A.LIMIET, A.AANTAL, A.STATUS, A.UITVSTATUS, A.STOPPX, A.TOT_AANT_UITGEVOERD, A.TOT_EFF_BEDR_UITGEVOERD, A.VERVALDATUM, A.INGANGSDATUM, A.ORDERTRAJECT, A.GELDIGHEIDSDUUR, A.PRIJS_FACTOR, A.AANTAL_CIJFERS_DISPLAY, A.OMS, A.ORIGIN, A.PENDING, A.ORINID, row_number() over (order by a.ordernummer :1) as line_number FROM od_orders_account_tt A where orderregel = 1) where line_number <= ~2 order by line_number :3 fetch first ~4 rows only"
552,,".5.2","Alleen DEPOT_ORDER_NR","select * from (select A.ORDERTYPE, A.ORDERNUMMER, A.ORDERREGEL, A.ADVISEUR, A.RELATIE, A.DEPOT, A.FONDSSYMBOOL, A.OPTIETYPE, A.EXPIRATIE, A.EXERCISE, A.TRANSACTIESOORT, A.ORDERSOORT, A.ORDERKEUZE, A.LIMIET, A.AANTAL, A.STATUS, A.UITVSTATUS, A.STOPPX, A.TOT_AANT_UITGEVOERD, A.TOT_EFF_BEDR_UITGEVOERD, A.VERVALDATUM, A.INGANGSDATUM, A.ORDERTRAJECT, A.GELDIGHEIDSDUUR, A.PRIJS_FACTOR, A.AANTAL_CIJFERS_DISPLAY, A.OMS, A.ORIGIN, A.PENDING, A.ORINID, row_number() over (order by a.ordernummer :1) as line_number FROM od_orders_account_tt A WHERE A.ORDERNUMMER>=~2 AND A.ORDERNUMMER<=~3 and orderregel = 1)  where line_number <= ~4 order by line_number :5 fetch first ~6 rows only"
552,,".5.3","Alleen DEPOT_PENDING","select * from (select A.ORDERTYPE, A.ORDERNUMMER, A.ORDERREGEL, A.ADVISEUR, A.RELATIE, A.DEPOT, A.FONDSSYMBOOL, A.OPTIETYPE, A.EXPIRATIE, A.EXERCISE, A.TRANSACTIESOORT, A.ORDERSOORT, A.ORDERKEUZE, A.LIMIET, A.AANTAL, A.STATUS, A.UITVSTATUS, A.STOPPX, A.TOT_AANT_UITGEVOERD, A.TOT_EFF_BEDR_UITGEVOERD, A.VERVALDATUM, A.INGANGSDATUM, A.ORDERTRAJECT, A.GELDIGHEIDSDUUR, A.PRIJS_FACTOR, A.AANTAL_CIJFERS_DISPLAY, A.OMS, A.ORIGIN, A.PENDING, A.ORINID, row_number() over (order by a.ordernummer :1) as line_number FROM od_orders_account_tt A WHERE  A.PENDING=~2 and orderregel = 1) where line_number <= ~3 order by line_number :4 fetch first ~5 rows only"
552,,".5.4","Alleen DEPOT_PENDING_INGANGSDATUM","select * from (select A.ORDERTYPE, A.ORDERNUMMER, A.ORDERREGEL, A.ADVISEUR, A.RELATIE, A.DEPOT, A.FONDSSYMBOOL, A.OPTIETYPE, A.EXPIRATIE, A.EXERCISE, A.TRANSACTIESOORT, A.ORDERSOORT, A.ORDERKEUZE, A.LIMIET, A.AANTAL, A.STATUS, A.UITVSTATUS, A.STOPPX, A.TOT_AANT_UITGEVOERD, A.TOT_EFF_BEDR_UITGEVOERD, A.VERVALDATUM, A.INGANGSDATUM, A.ORDERTRAJECT, A.GELDIGHEIDSDUUR, A.PRIJS_FACTOR, A.AANTAL_CIJFERS_DISPLAY, A.OMS, A.ORIGIN, A.PENDING, A.ORINID, row_number() over (order by a.ordernummer :1) as line_number FROM od_orders_account_tt A WHERE  A.PENDING=~2 AND A.INGANGSDATUM>=~3 AND A.INGANGSDATUM<=~4 and orderregel = 1) where line_number <= ~5 order by line_number :6 fetch first ~7 rows only"
552,,".5.5","Alleen DEPOT Laatste versie","select * from (select A.ORDERTYPE, A.ORDERNUMMER, A.ORDERREGEL, A.ADVISEUR, A.RELATIE, A.DEPOT, A.FONDSSYMBOOL, A.OPTIETYPE, A.EXPIRATIE, A.EXERCISE, A.TRANSACTIESOORT, A.ORDERSOORT, A.ORDERKEUZE, A.LIMIET, A.AANTAL, A.STATUS, A.UITVSTATUS, A.STOPPX, A.TOT_AANT_UITGEVOERD, A.TOT_EFF_BEDR_UITGEVOERD, A.VERVALDATUM, A.INGANGSDATUM, A.ORDERTRAJECT, A.GELDIGHEIDSDUUR, A.PRIJS_FACTOR, A.AANTAL_CIJFERS_DISPLAY, A.OMS, A.ORIGIN, A.PENDING, A.ORINID, row_number() over (order by a.ordernummer :1) as line_number FROM od_orders_account_tt A :2) where line_number <= ~3 order by line_number :4 fetch first ~5 rows only"
552,,".5.6","Laatste versie","select * from (select A.ORDERTYPE, A.ORDERNUMMER, A.ORDERREGEL, A.ADVISEUR, A.RELATIE, A.DEPOT, A.FONDSSYMBOOL, A.OPTIETYPE, A.EXPIRATIE, A.EXERCISE, A.TRANSACTIESOORT, A.ORDERSOORT, A.ORDERKEUZE, A.LIMIET, A.AANTAL, A.STATUS, A.UITVSTATUS, A.STOPPX, A.TOT_AANT_UITGEVOERD, A.TOT_EFF_BEDR_UITGEVOERD, A.VERVALDATUM, A.INGANGSDATUM, A.ORDERTRAJECT, A.GELDIGHEIDSDUUR, A.PRIJS_FACTOR, A.AANTAL_CIJFERS_DISPLAY, A.OMS, A.ORIGIN, A.PENDING, A.ORINID, row_number() over (order by a.ordernummer :4) as line_number FROM :1 A :2) where line_number <= ~3 order by line_number :5 fetch first ~6 rows only"
552,,".5.7","response_OrinLoop","select * from (select A.ORDERTYPE, A.ORDERNUMMER, A.ORDERREGEL, A.ADVISEUR, A.RELATIE, A.DEPOT, A.FONDSSYMBOOL, A.OPTIETYPE, A.EXPIRATIE, A.EXERCISE, A.TRANSACTIESOORT, A.ORDERSOORT, A.ORDERKEUZE, A.LIMIET, A.AANTAL, A.STATUS, A.UITVSTATUS, A.STOPPX, A.TOT_AANT_UITGEVOERD, A.TOT_EFF_BEDR_UITGEVOERD, A.VERVALDATUM, A.INGANGSDATUM, A.GELDIGHEIDSDUUR, A.PRIJS_FACTOR, A.AANTAL_CIJFERS_DISPLAY,  A.OMS, A.ORIGIN, A.PENDING, A.ORINID, row_number() over (order by a.ordernummer :1) as line_number FROM od_orders_account_tt A where  (~2=0 OR (~2<>0 AND ORDERNUMMER <= ~2 )) and orderregel = 1 ) where line_number <= ~3 order by line_number :4 fetch first ~5 rows only"
